import { randomSpeed } from '../helpers'

const checkDownloadSpeed = () => {
  return randomSpeed();
}
const checkUploadSpeed = () => {
  return randomSpeed();
}
export default {
  checkDownloadSpeed,
  checkUploadSpeed
}